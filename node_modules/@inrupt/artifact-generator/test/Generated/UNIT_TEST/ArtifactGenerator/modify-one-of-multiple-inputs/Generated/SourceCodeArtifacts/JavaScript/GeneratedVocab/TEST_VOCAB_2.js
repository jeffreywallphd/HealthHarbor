/**
 * Generated by the Inrupt Artifact Generator [@inrupt/artifact-generator], version [3.1.1]
 * as part of artifact: [generated-vocab-static-TEST], version: [10.11.12]
 * on 'Monday, October 2, 2023 11:19 AM'.
 *
 * Vocabulary built from vocab list file: [test/Generated/UNIT_TEST/ArtifactGenerator/modify-one-of-multiple-inputs/vocab-list-static.yml].
 * The generator detected the following terms in the source vocabulary:
 *  - Classes: [1]
 *  - Properties: [0]
 *  - Literals: [0]
 *  - Constant IRIs: [0]
 *  - Constant strings: [0]
 *
 * Testing static file won't cause re-generation.
 */

// We prefix our local variables with underscores to (hopefully!) prevent
// potential names clashes with terms from vocabularies.
const { VocabTerm: _VocabTerm, getLocalStore } = require("@inrupt/solid-common-vocab");
const _DataFactory = require("@rdfjs/data-model");

const { namedNode: _namedNode } = _DataFactory;

function _NS(localName) {
  return _namedNode(`https://inrupt.com/vocab/static-test-2/${localName}`);
}

/**
 * Testing static file won't cause re-generation.
 *
 * Namespace IRI: [https://inrupt.com/vocab/static-test-2/]
 */
const TEST_VOCAB_2 = {
  PREFIX: "test_vocab_2",
  NAMESPACE: "https://inrupt.com/vocab/static-test-2/",
  PREFIX_AND_NAMESPACE: { "test_vocab_2": "https://inrupt.com/vocab/static-test-2/" },
  NS: _NS,

  // *****************
  // All the Classes.
  // *****************

  /**
   * We have a label, but no comment!
   *
   * This term has a label (in language [NoLocale]), but no long-form descriptions at all (i.e., the vocabulary doesn't provide any 'rdfs:comment' or 'dcterms:description' metadata).
   */
  sampleTerm: new _VocabTerm(
    _NS("sampleTerm"),
    _DataFactory,
    getLocalStore(),
    false
  )
    .addType(_namedNode("http://www.w3.org/2000/01/rdf-schema#Class"))
    .addLabelNoLanguage(`We have a label, but no comment!`),

}

module.exports = TEST_VOCAB_2;
